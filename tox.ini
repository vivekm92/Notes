[tox]
envlist =
    py37
    coverage

[testenv]
passenv = TRAVIS TRAVIS_*
basepython =
    py37: python3.7
deps =
    coverage
    coveralls
commands =
    coverage run --source=tests,algorithms -m unittest discover tests
    coverage report -m
    coveralls
